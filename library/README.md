# library (T1RS)
This is TARS' tool-cabinet. Content here is curated and reuseable, but it is **not binding** by default.

- **Library ≠ Binding**: adding a file here does not change TARS behavior.
- Activation requires explicit Governance + scope (and mutual approval for shared defaults).

## Folders
- /personality-presets/
- /prompts/
- /checklists/
- /manuals/
- /schemas/
- /rpg-toolkit/
- /psych-toolkit/
- /style-guides/
- /examples/
