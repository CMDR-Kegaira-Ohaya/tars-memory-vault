# /ops - CONFLICT_PROTOCOL

Purpose: When a proposed rule conflicts with an existing shared rule or preference.

Menu options:
- Personal (local override)
- Replace Shared (requires both)
- Review (compare tradeoffs)
- Defer (innactive proposal)

# Guardrails
- No silent overwrite.
- No auto-promotion to shared.